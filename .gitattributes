# Keep Linux-destined files with LF endings even when checked out on Windows,
# so deploy scripts and configs work when copied to the server.
*.sh text eol=lf
*.service text eol=lf
*.conf text eol=lf
*.sample text eol=lf
*.sql text eol=lf
